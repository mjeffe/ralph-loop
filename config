# Ralph configuration

# Project directories
SPECS_DIR="specs"

# Loop configuration
DEFAULT_MAX_ITERATIONS=10
MAX_RETRIES=3

# Agent configuration
# AGENT_TYPE selects built-in presets for the agent CLI. Supported types:
#   amp, claude, cline, codex
# Each type sets sensible defaults for AGENT_CLI, AGENT_ARGS,
# AGENT_RESPONSE_FILTER, and AGENT_DISPLAY_FILTER.
# You can override any individual value below the AGENT_TYPE line.
AGENT_TYPE="amp"

# Override individual agent settings if needed:
#AGENT_CLI="amp"
#AGENT_ARGS="-x --dangerously-allow-all --stream-json-thinking"
#AGENT_RESPONSE_FILTER='select(.type=="assistant") | .message.content[] | select(.type=="text") | .text'
#AGENT_DISPLAY_FILTER="jq -r '...'"
