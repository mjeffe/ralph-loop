# Ralph session logs (generated, not committed)
logs/

# Generated each iteration by the loop
last_agent_output

# Upstream review files from ralph update
*.upstream

# Sandbox environment file (contains tokens)
sandbox/.env

# Sandbox sentinel files (created by entrypoint.sh inside the container)
.sandbox/

# Upstream originals for three-way merge (internal to updater)
.originals/
